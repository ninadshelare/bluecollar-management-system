spring:
  profiles:
    active: dev

  datasource:
    url: jdbc:mysql://localhost:3306/bluecollar_management
    username: root
    password: Roronoa25@
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8081
  
jwt:
  secret: bluecollar-secret-key-1234567890123456
  expiration: 86400000   # 1 day
